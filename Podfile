# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodList
  pod 'RxSwift', '~> 5.0'
  pod 'RxCocoa', '~> 5.0'
  pod 'Kingfisher', '5.6.0'

  target 'FoodListTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
